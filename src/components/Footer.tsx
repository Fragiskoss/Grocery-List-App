import React from 'react';
import { Box, Typography } from '@mui/material';

const Footer: React.FC = () => (
  <Box mt={5} mb={2} textAlign="center">
    <Typography variant="body2">
      © 2024 Grocery List Manager
    </Typography>
  </Box>
);

export default Footer;
